<!DOCTYPE html>
<html>
    <head>
        {% load staticfiles%}
        <link href="{% static "room/bootstrap/css/bootstrap.min.css" %}" rel="stylesheet">
        <title>{% block title %}Główna{% endblock%}</title></head>
<body style='padding-top:60px'>
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                         <li class="active"> <a href="{% url 'glowna' %}">Strona Główna</a></li>
                        <li><a href="{% url 'lista_pokoi' %}">Zarezerwuj Pokój</a></li>
                        <li><a href="{% url 'szukaj_pokoi' %}">Wyszukaj Pokój</a></li>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                    
                        
                        {% if user.is_authenticated %}
                           <li class='active'><a> {{ user.username }}</a></li><li> <a href="{% url 'logout' %}">wyloguj</a></li>
                        {% else %}
                        <li><form action="{% url 'login' %}" method="POST" class="navbar-form navbar-right" role="form">
                            
                            {% csrf_token %} 
                            <div class="form-group">
                            <input type="text" class="form-control" name="username" placeholder="Enter username">
                            </div>
                            <div class="form-group">
                            <input type="text" class="form-control" name="password" placeholder="Enter password">
                            </div>
                            <button type="submit" class="btn btn-default">Zaloguj</button>
                        </form></li>
                        {% endif %}
                        
                        
                    </ul>
                </div>
            </div>
        </div>
        
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <div class="panel panel-primary">
            <!-- Default panel contents -->
            <div class="panel-heading">{% block panelname %}Strona główna{% endblock %}</div>
            <div class='panel-body'>
                {% block content %} 
                Strona do rezerwowania pokoi
                {% endblock %}
                
                {% if messages %}
                <ul>
                    {% for message in messages %}
                    <li>{{ message }}</li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
        </div>
    </div>
</div>
<script src="{% static "jquery.min.js" %}"></script>
        
        <script src="{% static "room/bootstrap/js/bootstrap.min.js" %}"></script>
</body>
</html>